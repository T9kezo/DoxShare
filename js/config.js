// config.js — PocketBase configuration for Dox-Share
// ─────────────────────────────────────────────────
// Change pbUrl to your PocketBase server address.
// Default: http://127.0.0.1:8090  (local dev)
// Production: https://your-domain.com  (wherever you deploy PocketBase)

const config = {
    pbUrl: 'https://pocketbase-production-c772.up.railway.app',
};

export default config;
